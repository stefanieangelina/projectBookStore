<?php

use App\Book;
use Illuminate\Database\Seeder;

class BookSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //

        // factory(Book::class, 50)->create();
        // ->each(function ($book) {
        //     $book->posts()->save(factory(App\Post::class)->make());
        // });

        // Init Buku

    }
}
